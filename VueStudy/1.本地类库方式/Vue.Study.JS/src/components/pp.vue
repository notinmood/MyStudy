// E:\myworkspace\MyStudy\VueStudy\1.本地类库方式\vue_test_js\src\components
<script>
 const chunk = (arr, size) =>
   Array.from({ length: Math.ceil(arr.length / size) }, (v, i) =>
     arr.slice(i * size, i * size + size)
   );


</script>
<template>
    <div href=""> </div>
</template>